"""MultiLink Manager MVP v0.1.

A read-only Windows desktop application for observing multi-WAN / multi-link
network health: interface discovery, traffic counters, link probing,
scoring, and per-connection visibility.

This application never modifies routes, never connects/disconnects network
adapters, and never installs drivers. It is strictly observational.
"""

__version__ = "0.1.0"
