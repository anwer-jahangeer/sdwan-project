# Add project specific ProGuard rules here.
# By default, the flags in this file are appended to flags specified
# in ${sdk.dir}/tools/proguard/proguard-android-optimize.txt

# This app has no reflection-based frameworks, no JNI, and no hidden/private
# API usage, so no custom keep rules are required beyond the AGP defaults.
# Kotlin coroutines and Compose ship their own consumer ProGuard rules.

# Uncomment this to preserve line numbers for readable stack traces in
# crash reports, at a small size cost:
#-keepattributes SourceFile,LineNumberTable
#-renamesourcefileattribute SourceFile
